<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.nekotori.dao.ChatGroupMapper">
    <resultMap id="BaseResultMap" type="org.nekotori.entity.ChatGroupDo">
        <result column="id" jdbcType="INTEGER" property="id"/>
        <result column="group_id" jdbcType="BIGINT" property="groupId"/>
        <result column="group_name" jdbcType="VARCHAR" property="groupName"/>
        <result column="commands" jdbcType="VARCHAR" property="commands"/>
        <result column="is_block" jdbcType="BOOLEAN" property="isBlock"/>
        <result column="group_level" jdbcType="INTEGER" property="groupLevel"/>
    </resultMap>

    <sql id="baseResult">
        id, group_id, group_name, is_block, group_level, commands
    </sql>

    <insert id="insertChatGroup" parameterType="org.nekotori.entity.ChatGroupDo">
        <selectKey keyProperty="id" order="AFTER" resultType="java.lang.Integer">
            select LAST_INSERT_ROWID()
        </selectKey>
        insert into chat_group(id, group_id, group_name, is_block, group_level, commands)
        values (
        #{id,jdbcType=INTEGER},
        #{groupId,jdbcType=BIGINT},
        #{groupName,jdbcType=VARCHAR},
        #{isBlock,jdbcType=BOOLEAN},
        #{groupLevel,jdbcType=INTEGER},
        #{commands,jdbcType=VARCHAR}
        )
    </insert>

    <select id="selectRegisteredGroup" resultType = "java.lang.Long">
        select
        group_id
        from chat_group
    </select>

    <select id="selectGroupById" resultMap="BaseResultMap" parameterType="java.lang.Long">
        select
        <include refid="baseResult"/>
        from chat_group
        where group_id = #{gid}
        limit 1
    </select>
    <update id="updateChatGroup" parameterType="org.nekotori.entity.ChatGroupDo">
        update chat_group set
        group_id = #{groupId,jdbcType=BIGINT},
        group_name = #{groupName,jdbcType=VARCHAR},
        is_block = #{isBlock,jdbcType=BOOLEAN},
        group_level = #{groupLevel,jdbcType=INTEGER},
        commands = #{commands,jdbcType=VARCHAR}
        where
        id = #{id,jdbcType=INTEGER}
    </update>
</mapper>